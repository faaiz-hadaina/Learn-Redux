const TodoReducer = (state = 0, action) => {
    switch (action.type) {
        case "Add_Todo" :
            return state
        default :
            return state

    }
 }

export default TodoReducer